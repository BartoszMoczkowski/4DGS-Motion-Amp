# Docs compilation (2026-07-19)

Compiled the project's work into `docs/`: `README.md` (index), `overview.md`,
`motion-segmentation.md`, `omniverse-pipeline.md`, `orchestrator.md`, `memory-dump.md`
(assistant memory dump, Cowork-tooling entries excluded), alongside the pre-existing
`viewer_usage.md`. Sources: the three project notes files here, `orchestrator/planning/`,
the omniverse/orchestrator READMEs, and assistant memory. Root `README.md` now points to
`docs/` (also completed its previously truncated last sentence about `./output`).

Docs are summaries with pointers; these notes files remain the detailed chronological record.
When project state changes materially (e.g. T17 closed, segmentation quality on real data),
update `docs/overview.md` status and the relevant doc.
