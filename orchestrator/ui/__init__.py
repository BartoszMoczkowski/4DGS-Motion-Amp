"""Layer 3 — the Streamlit UI (T15).

Not meant to be imported as a library; run ``app.py`` directly with ``streamlit run ui/app.py``
(see ``README.md``). This file only exists so the ``ui`` directory is a proper Python package for
packaging purposes (``pyproject.toml``'s ``[tool.setuptools.packages.find]``), matching
``mcp_server``'s own convention.
"""
