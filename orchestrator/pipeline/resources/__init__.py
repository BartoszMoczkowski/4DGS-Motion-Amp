"""VRAM/RAM resource manager: pynvml/nvidia-smi query, serial gating, OOM-retry.

Stub — implemented in T12 (Resource manager). Must not import ``pynvml`` at
module scope so the package stays importable on machines/sandboxes with no GPU.
"""
