"""
camera-sync-analyzer

Post-processing tool that extracts frames from recorded MKV files,
decodes the QR timestamps and ArUco markers, fits per-camera time
offsets, and emits a sync report plus optionally remuxed aligned clips.
"""
